print ("Area del Terreno")

A = float(input("Area de A:"))
B = float(input("Area de B:"))
C = float(input("Area de C:"))
X = float(A - C)

AREA_TOTAL = (B * C) + ((B * X) / 2)

print ("AREA TOTAL DEL TERRENO", AREA_TOTAL)

        
